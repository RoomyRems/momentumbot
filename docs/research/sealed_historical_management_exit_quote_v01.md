# Historical management exit source reuse and metadata quote v0.1

This child of `e360af9289cfbfcd3e94dc7ff78b77828fb7c253` binds verified XAGE
source reuse to the unchanged runner exit plan and registers the remaining
80 metadata requests. It preserves the frozen 30 dates, all 109 opportunities,
86 available and 23 unavailable entry inputs, original entry references,
12 account paths and 360 session dependencies.

The hypothesis is that the one retained pair can be authenticated from complete
original bytes and the exact remaining scope can be quoted without substitution.
Ross transcripts, retrospective labels, fills and later outcomes do not enter
request selection or runtime reconstruction.

## Exact retained XAGE pair

The original execution acquisition run `34084113393` supplied the result ZIP
`1fe68f4e526caa6ce0ee8f8845248e5ef3039f3e3ffbce370020162add3b99d6`
and consumption ZIP
`105e3b93fb7be691d330e4e35451455352c5e64fc914ede3e7a253d4f769154a`.
All 144 and 17 original file members were verified, and the frozen full capture
verifier reproduced the original terminal result. Requests 78 and 79 contain
230,703 quote records and four status records for XAGE on 2025-07-15.

The entire compressed and normalized tapes, canonical record-list hashes,
original request receipts, native receive-time order, quote ordinals and status
vocabulary were checked. The quote request starts at `1752580565209819825` ns;
both requests end exclusively at `1752586973628965063` ns. They contain the
registered common exit interval ending at `1752581525309819825` ns. The
[reuse receipt](../../research/data-audits/sealed-historical-management-exit-quote-v0.1-xage-reuse.json)
retains exact byte and group commitments without changing the parent plan.

Reuse proves source integrity and interval containment. It does not assert that
every prospective exit has a fresh executable quote or unambiguous status.
Frozen per-exit availability and execution checks still apply. No slicing,
renormalization, fill simulation or resting quote inference is performed here.

## Bounded metadata quote

The unchanged 40 missing symbol/date groups require 40 `mbp-1` and 40 `status`
requests. Their ordered request-list commitment remains
`973a926e063fda7ec76883dd4297b88c77a06af22fe3bc0618330e98e28825b4`.
The verified XAGE pair is excluded from this quote.

The child inherits the pinned metadata transport and SDK routing; only the exact
request registration constructor changes. Each request permits
`get_billable_size`, then `get_cost`, once, for a maximum of 160 metadata calls
and 160 HTTP attempts. Endpoints, symbols, schemas and nanosecond bounds are
checked before HTTP. There are no retries, redirects or time-series calls.
Calls and HTTP attempts are journaled before access. Provider failures retain
sanitized codes; zero billable size is unavailable, and incomplete totals remain
unknown. A passing quote does not authorize a purchase.

Execution requires a sole newly added execution record whose immediate parent
has passing first-attempt CI and dedicated validation. The execution record binds
the full parent commit, tree, workflow hash, exact run IDs and contract. An atomic
new consumed tag and an uploaded consumption artifact precede the only credential
step. Reruns and changed execution records fail closed. Complete or partial quote
evidence is retained, including both ledgers and the exact XAGE reuse receipt.
The existing user authorization for continued development and operational steps
covers this metadata-only execution; provider spend remains zero.

## Verification and next gate

The independent stdlib checker imports no research implementation. It reads every
original ZIP member, checks every XAGE row and recomputes quote totals and order
from retained result and consumption ZIPs. Focused tests cover the pinned SDK,
transport boundaries, byte pinning, unchanged unavailable opportunities, complete
CLI evidence, credential preflight failure and independent rejection of rehashed
cost tampering. The full repository suite runs before publication.

```bash
PYTHONPATH=src:scripts python scripts/quote_sealed_historical_management_exit_inputs_v01.py --validate-only
python scripts/verify_sealed_historical_management_exit_quote_v01.py reuse --result-zip ORIGINAL_RESULT.zip --consumption-zip ORIGINAL_CONSUMPTION.zip
python scripts/verify_sealed_historical_management_exit_quote_v01.py quote --result-zip QUOTE_RESULT.zip --consumption-zip QUOTE_CONSUMPTION.zip
```

Next, a separately registered acquisition child must bind independently verified
size/cost values, hard re-quote ceilings, exact requests, normalization and its own
durable one-shot consumption. Historical execution remains blocked on complete
exit tapes, authenticated account-state production, historical fees and sell-ledger
reconciliation, causal next-session valuation, continuous account/order integration
and a separately authorized historical execution child.

## Verified operational result

Execution `fde309481843b7c2613d9a673f888f488a1ee795`, run `34169338681`,
completed successfully on attempt 1. All 80 requests were quoted through exactly
160 metadata calls and HTTP attempts, with no blocked attempts or retries.
The verified quote is **209,898,320 billable bytes for $0.234732925899 USD**.
Both verifiers checked the complete result and consumption ZIPs, every quote
value, full request/call order, byte inventory and exact successful code parent.
See the [permanent operational audit](../../research/data-audits/sealed-historical-management-exit-quote-v0.1-independent-verification-34169338681.json) and
[unchanged provider quote report](../../research/data-audits/sealed-historical-management-exit-quote-v0.1-report-34169338681.json).

All 1,888 local tests passed with zero skips; the 83 focused tests passed normally
and optimized locally and in the dedicated hosted environment. All eight code
workflows and both execution workflows passed on their first attempt. Main and
the 14 prior consumed references stayed unchanged; the new quote reference is
permanently consumed. No time-series acquisition, account replay or backtest ran.
