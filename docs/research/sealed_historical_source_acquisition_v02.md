# Sealed historical source acquisition repair v0.2

## Frozen failure parent

Run `33350635957` permanently consumed and failed the v0.1 authorization at
exactly 20,000 HTTP attempts. Its sanitized artifact proves the safety guard
blocked the next Alpaca request before network access. V0.2 does not rerun or
edit v0.1 and cannot reuse the incomplete ephemeral source tree.

## Single repair

V0.2 changes only the shared zero-cost HTTP-attempt ceiling from 20,000 to
40,000. The 30 selected dates, Massive membership census, identity and
corporate-action normalization, Alpaca SIP endpoints and parameters, SEC
enrichment, candidate ceiling, scanner reconstruction, canonical-input format,
retention ceiling, credentials, and all causal and strategy rules are
unchanged.

The larger ceiling is an operational safety-bound repair informed only by the
v0.1 provider-call accounting. It does not inspect a transcript, label, symbol
outcome, strategy decision, or P&L. All authorized providers remain
zero-incremental-cost and Databento remains prohibited.

## One-shot behavior

The v0.2 authorization is consumed before provider access and permits only
workflow attempt 1. A failed or cancelled v0.2 run cannot be automatically
rerun. Only a complete, hash-validated source bundle may pass; on failure the
workflow preserves sanitized host-level request counts and completion counts,
not provider rows or a partial source tree.
