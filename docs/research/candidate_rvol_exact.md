# Exact-minute candidate RVOL history

Hosted source integration `35169713629` succeeded on `b7494ec`. All 3,778
eligible cases were projected: 3,770 match in every target volume bucket and
eight differ (NCT on seven dates, JAGX on one). All eight remain in the exact
capture. The frozen conservative filter retains 2,103 cases and excludes 1,675
without a possible event under the unchanged necessary price/time/RVOL rule,
reducing the history population by 44.3%. The older 240 empty/short-history
exclusions remain unchanged and explicit in the coarse parent.

Parent full CI `35169713635` passed 3,049 tests in 529.575 seconds with 73
optional skips and successful compilation.

The accepted original integration artifact is `10475623243`, 312,667 bytes,
SHA-256 `d84c6c08be1c0dd0e85fa5ffbfb996a4f70a707c33d87f1e0b2e662fb58dcbaa`.
Its report seal is
`203811a87136c8a2dd1768519eb57b5452ed81578de45a84f45ee7e4e0a4be14`.
The original ZIP, seven normal/optimized source tests, full diagnostic and
source metadata are retained in the candidate-RVOL-source audit directory.

The completed source integration supplies a conservative exact-minute request
plan. This stage binds its original hosted artifact and final content seal,
re-derives every request from the unchanged coarse roots and retained symbols,
and requires an exact match before registration or capture.

Every retained candidate/date receives fifty unique prior observed sessions.
Requests use split-adjusted SIP 1Min bars from 04:00 through 09:59 ET and retain
the target date's asof entity mapping. There are 1,500 history roots with
105,150 symbol/session observations. Eight additional roots refresh only the
unresolved target mornings, allowing a same-capture adjustment-basis comparison;
all other accepted target-minute data is reused. The original discrepant data
is retained. A new target sample is not assumed to resolve the discrepancy
until the exact source integration checks it.
The interval, adjustment and entity-mapping meanings follow the provider's
[historical bars documentation](https://docs.alpaca.markets/us/reference/stockbars).

The protocol preserves strict original OHLC/page checks and per-symbol time
ordering, and additionally requires each bar to belong to the requested prior
observation date. It reuses the hosted one-use consumption, exact-code CI,
credential isolation, bounded transport and independent original-archive replay.
Limits remain 9,000 total attempts, 20 pages per root, 1.5 GB payload, 80 MB
metadata and 150 minutes of capture, without retry. These are ceilings, not
predicted usage. A failed capture remains consumed and its original data is kept.

The resulting history is an input for exact same-time RVOL. Capture success
alone does not enable the historical scanner or account evaluation; source
consistency and the existing runtime adapters still need integration. The
coarse filter never becomes a scanner feature, and all target-volume discrepancies
remain in the exact download population. Frozen policy and risk are unchanged.

All five focused exact-capture tests pass normally and optimized; compilation
also passes. They cover strict timestamps, original archive
roundtrip, authenticated prepared requests, tampered evidence, target-only
rechecks and preflight size. Full CI and the new one-use capture are triggered
by publication; success is not claimed until their hosted results are accepted.
