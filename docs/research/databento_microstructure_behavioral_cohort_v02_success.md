# Databento microstructure behavioral cohort v0.2 success

## Status

Completed and consumed. The sole parent-bound execution authorization triggered
workflow `32575593240`, attempt 1, which completed successfully on August 22,
2026. It must not be reused or rerun.

## Verified result

All five frozen requests were quoted before the first download at an aggregate
`$0.078219142556` and `69,989,304` billable bytes, below the fixed `$0.25` and
`225,000,000`-byte ceilings. Actual billing remains unknown.

Every request succeeded, all 10 accepted-panel opportunities were retained,
and an independent feature engine reproduced the complete comparison sequence
exactly across all registered one-, five-, and ten-second windows. All horizons
were reported together. No horizon, feature, threshold, execution scenario, or
policy was selected.

The permanent audit is
`research/data-audits/databento-microstructure-behavioral-cohort-v0.2-run-32575593240-success-2026-08-22.json`.
It binds the workflow, authorization, artifact digests, request summaries,
aggregate evidence, and safety checks. Raw data and per-opportunity feature
values were deleted rather than published.

## Consumed authority gate

The v0.2 authorization was the sole change in a direct child of
`fed2b11c9a55ad45e340d80a4dbee8326ae193a3`. Its first-attempt-only authority
is consumed. Workflow `32575593240` cannot be retried, and the authorization
cannot authorize another provider request.

## Claim boundary and next gate

The result proves deterministic label-blind engineering on the frozen
accepted-panel slice. It does not prove predictive value, hidden buying or
selling, a preferred horizon, realistic fills, generalization, or
profitability.

The next gate is a threshold-free shadow bridge to the separately registered
prospective management and execution scenarios. That bridge may carry only the
frozen cohort identity, sanitized aggregate direction counts, exact digests,
and unavailable states. It must report all horizons and both execution
scenarios together and cannot create runtime or broker authority.
