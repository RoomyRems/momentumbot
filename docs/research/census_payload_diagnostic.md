# Bounded census payload diagnostic

Completed successfully in run `34715001958` after CI `34715001971`. Exactly one
HTTP-200 response was retained. Its 1,000 provider-native tickers are correctly
ordered; uppercasing before validation creates eleven false regressions. Original
artifacts, byte verification and the isolated protocol repair are documented in
[census order repair](census_order_repair.md). This diagnostic is permanently
consumed and is not a full-census success or reusable capture authorization.

The immutable parent is `7e405354a5f24a0005cc7628a542acc0c23a29d6` and its
verified two-request census failure. This diagnostic tests one hypothesis:
credential-safe response retention and fixed rejection reasons can identify the
failure without weakening the frozen parser or calling the rest of the census.

The owner's September 12 continuation and process-streamlining instruction
authorizes this one diagnostic. The exact original first-page envelope remains
unchanged: Massive ticker reference, March 4, 2026, US stocks, active=true,
ascending ticker order, limit 1000. There is no pagination, retry, alternate
credential, subscription change, metered purchase or strategy execution.
The old census and old provider-check consumption refs remain untouched.

## Shorter delivery process, same substantive safeguards

Focused local tests cover the changed component and its adapter, including
optimized Python for explicit safety checks. One full GitHub CI run for the
exact code commit is authoritative. The diagnostic workflow runs alongside CI,
waits for that exact CI to succeed, and checks every test/compile step before
creating a permanent consumption ref. This removes a duplicate local full suite
and the separate approval/execution-only commit for this bounded diagnostic.

The registration pins its own source, tests, workflows, inherited source bytes
and the failed capture. A clean first-attempt push on the research branch must
be an exact child of the frozen failure checkpoint. After CI, the workflow
creates its separate consumption ref and uploads byte-pinned preflight evidence.
Only the dependent read-only diagnostic job receives the provider key. It checks
the live ref, artifact/run/commit identity, independent preflight pin and exact
runtime before reading the key. The provider key is never read during CI.

This is an authorization and diagnostics change, not a relaxation of data,
causality, risk or financial acceptance criteria. The owner-approved local
historical baseline replay waiver remains in effect.

## Response evidence

Only a complete, bounded, identity-encoded HTTP-200 JSON response may be retained.
Raw bytes and every decoded JSON string are checked for literal, encoded and
Unicode-escaped credential forms. Duplicate object-key values are retained in
the inspection traversal, so an overwrite cannot conceal a credential echo.
Unsafe or uninspectable responses produce only bounded receipts and hashes.

Safe JSON is saved before the unchanged legacy parser is called. A finite
allowlist exposes fixed schema/filter/order/cursor rejection labels; unexpected
exception text is never recorded. The report distinguishes successful diagnosis
from legacy acceptance. A green diagnostic run does not mean a complete census.
The response is quarantined research evidence, not approved runtime input.

The public [endpoint documentation](https://massive.com/docs/rest/stocks/tickers/all-tickers)
includes this endpoint in Basic with two years of history. Estimated incremental
API cost is USD 0.00; account-specific billing and the credit balance remain
unverified. No transcript records, recap actions, fills or later outcomes enter
the diagnostic. The strategy and its preserved losses are unchanged.

## Local verification

All 61 focused tests passed normally and optimized (25 new diagnostic tests plus
36 existing adapter tests). An initial new test-fixture construction error was
corrected before publication; it did not reach a provider. No local full-suite
rerun is required under the owner's streamlined process. The exact code commit
must pass the complete GitHub suite before the diagnostic can execute.
