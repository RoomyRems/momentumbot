name: Sealed historical management mechanics v0.1

on:
  push:
    branches: [phase-3-historical-snapshot]
    paths:
      - .github/workflows/sealed-historical-management-projection-v01.yml
      - src/momentumbot/research/sealed_historical_management_projection_v01.py
      - scripts/build_sealed_historical_management_projection_v01.py
      - tests/test_sealed_historical_management_projection_v01.py
      - research/strategy/sealed-historical-management-projection-v0.1.json
      - research/runtime/sealed-historical-management-projection-v0.1/**
      - requirements-sealed-execution-quote-v01.txt

permissions:
  contents: read

concurrency:
  group: sealed-historical-management-projection-v01-validation
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      PYTHONPATH: src:scripts
      PYTHONDONTWRITEBYTECODE: "1"
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38
        with:
          python-version: "3.12.14"
      - name: Install hash-locked validation environment
        run: |
          python -m pip install --disable-pip-version-check --require-hashes --only-binary=:all: --no-deps -r requirements-sealed-execution-quote-v01.txt
          python -m pip install --disable-pip-version-check --no-deps --no-build-isolation -e .
          python -m pip check
      - name: Reconstruct registration metadata offline
        run: python scripts/build_sealed_historical_management_projection_v01.py --verify
      - name: Verify synthetic causal mechanics and frozen execution dependencies
        run: |
          python -m unittest tests.test_sealed_historical_management_projection_v01 tests.test_prospective_management_window tests.test_execution_realism tests.test_sealed_historical_record_order_v01 -v
          python -O -m unittest tests.test_sealed_historical_management_projection_v01 tests.test_prospective_management_window tests.test_execution_realism tests.test_sealed_historical_record_order_v01 -v
          python - <<'PY'
          from pathlib import Path
          from check_recovery_entrypoints_v13 import undefined_globals
          for name in ('scripts/build_sealed_historical_management_projection_v01.py', 'src/momentumbot/research/sealed_historical_management_projection_v01.py'):
              missing = undefined_globals(Path(name).read_text(), name)
              if missing:
                  raise SystemExit(f'{name}: {sorted(missing)}')
          PY
